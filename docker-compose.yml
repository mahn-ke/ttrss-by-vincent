#http 
version: '3'

services:
  init-volume:
    image: busybox
    volumes:
      - plugins-volume:/data
      - ./plugins.local:/plugins.local:ro
    entrypoint: ["sh", "-c", "cp -r /plugins.local/. /data/"]
    restart: "no"

  db:
    image: postgres:17.9-alpine
    restart: unless-stopped
    environment:
      - POSTGRES_USER=${TTRSS_DB_USER}
      - POSTGRES_PASSWORD=${TTRSS_DB_PASS}
      - POSTGRES_DB=${TTRSS_DB_NAME}
      - TTRSS_PLUGINS=auth_internal,auth_oidc
      - TTRSS_AUTH_OIDC_NAME=${OAUTH_NAME}
      - TTRSS_AUTH_OIDC_URL=${OAUTH_URL}
      - TTRSS_AUTH_OIDC_CLIENT_ID=${OAUTH_CLIENT_ID}
      - TTRSS_AUTH_OIDC_CLIENT_SECRET=${OAUTH_CLIENT_SECRET}
      - TTRSS_AUTH_OIDC_VALIDATE_INTERVAL=${TTRSS_AUTH_OIDC_VALIDATE_INTERVAL}
      - ADMIN_USER_PASS=${ADMIN_USER_PASS}
    volumes:
      - db:/var/lib/postgresql/data

  app:
    image: cthulhoo/ttrss-fpm-pgsql-static:latest@sha256:ef867182f55c1218ce2889df7da31a801b0f7ff170974d1536c95c1d89d3e86c
    restart: unless-stopped
    volumes:
      - app:/var/www/html
      - plugins-volume:/var/www/html/tt-rss/plugins.local
    depends_on:
      - db
    environment:
      - TTRSS_DB_USER=${TTRSS_DB_USER}
      - TTRSS_DB_PASS=${TTRSS_DB_PASS}
      - TTRSS_DB_NAME=${TTRSS_DB_NAME}
      - TTRSS_PLUGINS=auth_internal,auth_oidc
      - TTRSS_AUTH_OIDC_NAME=${OAUTH_NAME}
      - TTRSS_AUTH_OIDC_URL=${OAUTH_URL}
      - TTRSS_AUTH_OIDC_CLIENT_ID=${OAUTH_CLIENT_ID}
      - TTRSS_AUTH_OIDC_CLIENT_SECRET=${OAUTH_CLIENT_SECRET}
      - TTRSS_AUTH_OIDC_VALIDATE_INTERVAL=${TTRSS_AUTH_OIDC_VALIDATE_INTERVAL}
      - ADMIN_USER_PASS=${ADMIN_USER_PASS}

  updater:
    image: cthulhoo/ttrss-fpm-pgsql-static:latest@sha256:ef867182f55c1218ce2889df7da31a801b0f7ff170974d1536c95c1d89d3e86c
    restart: unless-stopped
    volumes:
      - app:/var/www/html
      - plugins-volume:/var/www/html/tt-rss/plugins.local
    depends_on:
      - app
    environment:
      - TTRSS_DB_USER=${TTRSS_DB_USER}
      - TTRSS_DB_PASS=${TTRSS_DB_PASS}
      - TTRSS_DB_NAME=${TTRSS_DB_NAME}
      - TTRSS_PLUGINS=auth_internal,auth_oidc
      - TTRSS_AUTH_OIDC_NAME=${OAUTH_NAME}
      - TTRSS_AUTH_OIDC_URL=${OAUTH_URL}
      - TTRSS_AUTH_OIDC_CLIENT_ID=${OAUTH_CLIENT_ID}
      - TTRSS_AUTH_OIDC_CLIENT_SECRET=${OAUTH_CLIENT_SECRET}
      - TTRSS_AUTH_OIDC_VALIDATE_INTERVAL=${TTRSS_AUTH_OIDC_VALIDATE_INTERVAL}
      - ADMIN_USER_PASS=${ADMIN_USER_PASS}
    command: /opt/tt-rss/updater.sh

  web-nginx:
    image: cthulhoo/ttrss-web-nginx:latest@sha256:c064fc0e5865f81d2c4e244768488eabaa1340a9f2b6119aaf239df300095bf0
    restart: unless-stopped
    environment:
      - TTRSS_DB_USER=${TTRSS_DB_USER}
      - TTRSS_DB_PASS=${TTRSS_DB_PASS}
      - TTRSS_DB_NAME=${TTRSS_DB_NAME}
      - TTRSS_PLUGINS=auth_internal,auth_oidc
      - TTRSS_AUTH_OIDC_NAME=${OAUTH_NAME}
      - TTRSS_AUTH_OIDC_URL=${OAUTH_URL}
      - TTRSS_AUTH_OIDC_CLIENT_ID=${OAUTH_CLIENT_ID}
      - TTRSS_AUTH_OIDC_CLIENT_SECRET=${OAUTH_CLIENT_SECRET}
      - TTRSS_AUTH_OIDC_VALIDATE_INTERVAL=${TTRSS_AUTH_OIDC_VALIDATE_INTERVAL}
      - ADMIN_USER_PASS=${ADMIN_USER_PASS}
    ports:
      - 8281:80
    volumes:
      - app:/var/www/html
    depends_on:
      - init-volume
      - app

volumes:
  db:
  app:
  backups:
  plugins-volume:
